# ──────────────────────────────────────────────────────────────────────────────
# OpticBin — Development & Testing Dependencies
# ──────────────────────────────────────────────────────────────────────────────
# Install alongside core deps:
#   pip install -r requirements.txt -r requirements-dev.txt
#
# Or just dev tools:
#   pip install -r requirements-dev.txt

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0          # Coverage reports
pytest-timeout>=2.1.0       # Per-test timeout safety

# Linting & Formatting
flake8>=6.1.0               # PEP-8 style enforcement
black>=23.9.0               # Auto-formatter
isort>=5.12.0               # Import sorting

# Type Checking
mypy>=1.5.0

# YAML Support (for config/opticbin.yaml)
PyYAML>=6.0.1

# Memory Profiling (optional, used in benchmark.py)
psutil>=5.9.0

# Notebook / Reporting (optional)
matplotlib>=3.7.0           # Confusion matrix plots in evaluate.py
